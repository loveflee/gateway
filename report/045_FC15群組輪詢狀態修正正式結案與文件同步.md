# 045 — FC15 群組輪詢狀態修正：正式結案與文件同步

## 人話結論

042 抓到的假狀態已修正：單獨用 FC05 改變群組內的一路時，群組不會再顯示舊的安全狀態；它會先顯示 `__UNMATCHED__`，等完整 FC01 巡檢後才依真實 8 路 coil 重算。Claude Code 已獨立重打死亡案例、實機 FC15 與完整回歸，全部 PASS。Relay 已恢復全 OFF，Gateway 健康運行，因此本案正式 PASS。

## 本輪施工與驗證鏈

1. 施工者修改 `adapters/generic_adapter.py`：正常 FC01 poll 會重算引用該 command 的 `coil_groups`；FC05 單路 verify 會將受影響群組標示為 `__UNMATCHED__`，而不沿用舊快取。
2. `profile/relay_8ch_map2.yaml` 將 `__UNMATCHED__` 列為群組 select 的 display-only option；Adapter 拒絕將其編碼成 FC15 寫入。
3. 施工者自驗載於 [043](043_FC15群組輪詢狀態修正施工者自驗.md)；它不是正式裁決依據。
4. Claude Code 獨立驗收載於 [044](044_FC15群組輪詢狀態修正敵對獨立驗收_Claude.md)：全新 119 項 isolated harness、RTU/MBAP/PyModbus oracle、TCP ACK guard、FC01/05/06/16 regression、HA/MQTT observable、MQTT → Gateway → UID3 實機死亡案例與 FC15 真實切換皆 PASS。

## 042 死亡案例結果

```text
group_234=all_off
→ FC05 switch_2=ON
→ immediate verify: group_234=__UNMATCHED__
→ natural FC01 poll: group_234=__UNMATCHED__
→ FC05 switch_2=OFF
→ next natural FC01 poll: group_234=all_off
```

沒有任何 MQTT state payload 同時出現 `switch_2=ON` 與 `group_234=all_off`。完整 TX/RX、MQTT payload 與 pcap 證據見 044 第 5、6 節。

## 文件同步

在獨立 PASS 後才修改 `AGENTS.md` 與 `README.md`，補充：正常 FC01 poll 重算群組、單路 FC05 的暫時 unknown 語意、`__UNMATCHED__` 不能作為寫入命令，以及 FC15/FC05 的既有分工。沒有更動任何 production 邏輯、runtime config、原始 relay profile 或 HA/MQTT 既有 switch 契約。

最終健康檢查：`ginlong` 為 `running`、`RestartCount=0`；UID3 只有 Gateway 的單一 TCP master 連線；8 路 relay 全 OFF。

## 已知後續項目

042/044 已記錄兩項非本輪阻斷、未改動的項目：未引用的 `*_test.yaml` profile 仍會出現在 WebUI 選單，以及 `_pending_group_states` 尚未清除。兩者需另案評估，不影響本次死亡案例修復的 PASS。

## 最終裁決

```text
FC15 relay group poll-state fix: PASS
Independent adversarial acceptance: PASS
Final restore: PASS
Documentation updated after validation: YES
FINAL: PASS
```
