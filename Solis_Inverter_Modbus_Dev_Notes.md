# 錦浪光伏並網逆變器 Modbus RTU 開發注意事項與底層踩坑指南

## 1. 協議標準性
這是標準的 Modbus RTU 協議，跑在標準的 RS485 物理層上。
* **通訊參數**：9600 bps，8資料位，無校驗 (None)，1停止位。
* **功能碼**：只開放 `03H` (讀保持)、`04H` (讀輸入)、`06H` (寫單個)、`10H` (寫多個)。
* **異常處理**：錯誤碼回覆為標準的最高位加 128 (如 `0x80 + 0x04 = 0x84`)。無廠商自定義的特規封包結構。

## 2. 讀寫限制
* **讀取限制**：手冊未明文限制一次最多能讀多少個暫存器，通常受限於標準 Modbus RTU 單幀 256 bytes 上限（約可讀 125 個暫存器）。
* **寫入限制**：一幀 Modbus 僅支持單次寫，官方強烈推薦使用 `06H`（寫單個保持暫存器）功能碼來操作。

## 3. 特殊之處與地雷 (Gateway 開發必看)
* **硬體響應遲緩 (通訊間隔極慢)**：
  * 查詢指令（讀）間隔必須 **>300ms**。
  * 控制指令（寫）間隔必須 **>700ms**。
  * Python 輪詢迴圈 (`time.sleep`) 必須拉長，否則逆變器通訊模組會直接當機或無回應。
* **假正常 (不拋出 Exception)**：
  * **讀未實現暫存器**：直接返回 `0x0000`，不拋異常。讀到 0 必須自行判斷是真 0 還是空位址。
  * **寫未實現暫存器**：數據直接丟棄，但仍回覆正常響應（假陽性）。你會以為寫入成功，其實根本沒寫進去。
* **廣播靜默機制**：
  * 對 `0xFF` 廣播地址下達「寫入」指令後，從機只會執行動作，**不會回覆任何響應**。針對 `0xFF` 發送後必須直接放行，不能等待 ACK，否則程式死於 Timeout。
* **Flash 寫入壽命限制 (防寫入損耗)**：
  * 涉及掉電保存的設定（如 3068 掉電保存開關、3069/3070 啟停開關、有功無功功率設定），手冊多次警告建議不要頻繁設置。動態功率調節絕對不能放在高頻輪詢迴圈裡，會把逆變器內部的 Flash 記憶體刷廢。

## 4. 資料解析底層邏輯
### 4.1 嚴格的大端序 (Big-Endian) 處理
手冊明確標示所有的 `U32` 與 `S32` 數據（例如總發電量、總有功功率）都是「高位在前，低位在後，高位字在前，低位字在後」。這代表它是標準的 Big-Endian (大端序)。

### 4.2 有符號 (S16/S32) 與無符號 (U16/U32) 混用
手冊定義了 `S16`（有符號 16 位元）與 `S32`（有符號 32 位元）。
這點極度致命。例如「逆變器模組溫度 (3041)」與「實時功率因數 (3059)」都是 `S16`。如果你在 Python 裡統一當成無符號 `U16` 處理，當冬天溫度降到零下，或者功率因數為負值時，你會讀到 65535 這種荒謬的數值。解包時務必根據手冊查表，該用 `decode_16bit_int()` 的絕對不能用 `decode_16bit_uint()`。

### 4.3 特殊的比例因子 (Multiplier)
獲取資料後，必須除以手冊標示的單位：
* **百分比數值**：有功/無功功率百分比（3049, 3052），讀到 10000 代表 100.00%，倍率是 0.01。
* **功率因数 (PF)**：讀到 800 代表 0.8，倍率是 0.001。
* **電流/電壓/溫度**：單位多為 0.1A、0.1V、0.1 °C，讀到的整數必須乘以 0.1。

### 4.4 單一暫存器夾帶雙數據 (高低 Byte 拆解)
這台機器的版本號不走常規的 ASCII 字串，而是把兩個數據塞進一個 16 Bit 暫存器。
例如「DSP 軟體版本 (3000)」，高位元組是從 DSP 版本，低位元組是主 DSP 版本。
